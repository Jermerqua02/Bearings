import ComingSoon from "@/components/ui/ComingSoon";

export default function ListPage() {
  return (
    <ComingSoon
      label="My list"
      title="A balanced list"
      muted="is the whole game."
      description="Reach / Target / Likely columns with drag-and-drop, a balance meter with plain-language warnings, deadline countdowns, and side-by-side compare. Arrives in a coming pass."
    />
  );
}
