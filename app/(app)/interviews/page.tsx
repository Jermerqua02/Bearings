import ComingSoon from "@/components/ui/ComingSoon";

export default function InterviewsPage() {
  return (
    <ComingSoon
      label="Interviews"
      title="Practice the conversation"
      muted="before it counts."
      description="Mock interviews with the AI per school, question banks, feedback on substance — plus a primer on what interviews are actually for, which most students misunderstand. Arrives in a coming pass."
    />
  );
}
