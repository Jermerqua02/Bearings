import ComingSoon from "@/components/ui/ComingSoon";

export default function ApplyPage() {
  return (
    <ComingSoon
      label="Apply"
      title="Applications, essays, recommenders"
      muted="— one calm surface."
      description="Universal profile entered once, per-school requirements, an essay workspace where the AI critiques but never writes, recommender tracking, submission pipeline, and a financial aid tracker. Arrives in a coming pass."
    />
  );
}
