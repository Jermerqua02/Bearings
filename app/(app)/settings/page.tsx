import ComingSoon from "@/components/ui/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      label="Settings"
      title="Privacy is a product surface,"
      muted="not a policy page."
      description="Edit what the counselor remembers, control exactly what a linked parent can see, export your data, delete your account. Most of our users are minors; we never sell or share student data — and the UI will say so, not just the terms. Arrives in a coming pass."
    />
  );
}
